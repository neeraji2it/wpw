    <div class="text-right">
<%#= nested_notes @notes.arrange(:order => :created_at) %>
</div>

<%= render "form" %>

<table class="table table-bordered table-striped">

  <thead>
    <tr>
      <th>Name</th>
      <th>Subject</th>
      <th>Description</th>	
      <th>Download Link</th>
      <th>&nbsp;</th>
    </tr>
  </thead>
  <tbody>
    <% @notes.each do |note| %>
      <tr>
        <td><%= note.name %></td>
        <td><%= note.subject %></td>
        <td><%= note.description %></td>
        <td><%= link_to "Download note", note.attachment_url %></td>

        <td><%= button_to "Delete",  note, method: :delete, class: "btn btn-danger", confirm: "Are you sure that you wish to delete #{note.name}?" %>
        <%= note.created_at.strftime("%B %d, %Y ") %>
            </td>
      </tr>
    <% end %>

  </tbody>
</table>
<style>
   div.text-right{
     float: right;
}
</style>


 